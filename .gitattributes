# Auto detect text files and perform LF normalization
* text=auto

# Treat all JavaScript files as text
*.js text

# Treat all HTML files as text
*.html text

# Treat all CSS files as text
*.css text

# Treat all JSON files as text
*.json text

# Treat all image files as binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary

# Ignore node_modules directory when exporting
node_modules/ export-ignore
